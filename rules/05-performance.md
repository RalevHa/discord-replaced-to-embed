# 05 · Performance

- Choose an algorithm appropriate to the data size.
- Reduce repeated database queries (watch for N+1).
- Avoid unnecessary loops.
- Use async where appropriate — do not block other work.
- Measure before optimizing; optimize the real bottleneck, not a guess.
