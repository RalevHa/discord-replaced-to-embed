{
  "name": "discord-link-replacer-bot",
  "version": "1.0.0",
  "description": "Replaces Facebook/TikTok links with embeddable alternatives",
  "main": "index.js",
  "scripts": {
    "start": "node --env-file=.env index.js",
    "dev": "node --watch --env-file=.env index.js"
  },
  "dependencies": {
    "discord.js": "^14.15.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
