{
  "name": "discord-link-replacer-bot",
  "version": "1.0.0",
  "description": "Replaces social media links (TikTok, X, Bilibili, Pixiv, Bluesky, Instagram) with embeddable alternatives",
  "main": "index.js",
  "scripts": {
    "start": "node --env-file=.env index.js",
    "dev": "node --watch --env-file=.env index.js",
    "test": "node --test",
    "build:admin": "npm --prefix admin-panel install && npm --prefix admin-panel run build"
  },
  "dependencies": {
    "@simplewebauthn/server": "^13.3.2",
    "@upstash/redis": "^1.38.0",
    "cookie-parser": "^1.4.7",
    "discord.js": "^14.26.4",
    "express": "^5.2.1",
    "express-session": "^1.19.0",
    "pm2": "^7.0.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
